{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Links","text":"<p>Raffle Picker</p> <p>dotfiles</p>"},{"location":"Linux%20Host/","title":"Setting up a new Linux host","text":""},{"location":"Linux%20Host/#debian","title":"Debian","text":"<p>First steps:</p> <pre><code>su -\napt update\napt full-upgrade\nusermod -aG sudo barry\n</code></pre> <p>For sudo access with no password:</p> <pre><code>echo \"%sudo  ALL=NOPASSWD: ALL\" &gt; /etc/sudoers.d/sudo_group_nopasswd\n</code></pre> <p>Reboot.</p> <p>Convenient packages:</p> <pre><code>sudo apt install vim-gtk3 htop neofetch tmux curl git ripgrep fd-find npm\n</code></pre>"},{"location":"Linux%20Host/#virtualbox","title":"Virtualbox","text":"<pre><code>sudo apt install linux-headers-amd64 perl make gcc\n</code></pre> <p>Add <code>exec</code> for cdrom in <code>/etc/fstab</code>.</p> <p>Mount guest additions CD image and run <code>autorun.sh</code>.</p>"},{"location":"Linux%20Host/#opensuse","title":"openSUSE","text":"<p>For <code>ssh</code> and Windows DNS to work, load YaST &gt; Firewall and add <code>ssh</code> and <code>mdns</code> services to the <code>public</code> zone.</p>"},{"location":"Linux%20Host/#aliases","title":"Aliases","text":"<p>On openSUSE, <code>.bash_aliases</code> isn't picked up, instead it uses <code>~/.alias</code> so run</p> <pre><code>ln -s ~/.bash_aliases ~/.alias\n</code></pre>"},{"location":"Linux%20Host/#dircolors","title":"Dircolors","text":"<p>If <code>~/.dircolors</code> isn't working, add to <code>~/.bashrc</code>:</p> <pre><code>eval \"$(dircolors -b ~/.dircolors)\"\n</code></pre>"},{"location":"Linux%20Host/#prompt","title":"Prompt","text":"<p>If prompt is not as expected, add to <code>~/.bashrc</code>:</p> <pre><code>export PS1='\\[\\033[01;32m\\]\\u@\\h\\[\\033[00m\\]:\\[\\033[01;34m\\]\\w\\[\\033[00m\\]\\$ '\n</code></pre>"},{"location":"Linux%20Host/#config","title":"Config","text":"<pre><code>curl https://raw.githubusercontent.com/barrycohen/dotfiles/master/Sync/config-init.sh | bash\n</code></pre> <p>We rely on <code>.bashrc</code> importing <code>.bash_aliases</code> to set up the <code>config</code> command. This is the default in Debian.</p> <p>Now use <code>config</code> instead of <code>git</code> to manage the repository.</p> <p>In order to push changes, <code>config remote</code> should be changed to <code>git@github.com:barrycohen/dotfiles.git</code> and the ssh public key should be added to github to allow push.</p> <pre><code>ssh-keygen\ncat ~/.ssh/id_rsa.pub\nconfig remote set-url origin git@github.com:barrycohen/dotfiles.git\n</code></pre> <p>Log into github, <code>settings</code> &gt; <code>SSH and PGP keys</code> &gt; <code>New SSH key</code>.</p> <p>github does not allow an ssh key to be used for multiple repositories. If you need a separate one, run <code>ssh-keygen</code> and save they key to <code>~/.ssh/id_rsa_config</code>. Add <code>id_rsa_config.pub</code> to github instead. Then configure git to use this key with:</p> <pre><code>config config core.sshCommand \"ssh -i ~/.ssh/id_rsa_config\"\n</code></pre>"}]}